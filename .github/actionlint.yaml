self-hosted-runner:
  labels:
    # Standard GitHub-hosted Intel runner; actionlint 1.7.7 predates the label.
    - macos-15-intel
    # Standard GitHub-hosted Windows on ARM runner; same story.
    - windows-11-arm
